diff --git a/conversion/base.py b/conversion/base.py
index 9fba5a8..ac83024 100644
--- a/conversion/base.py
+++ b/conversion/base.py
@@ -1188,9 +1188,13 @@ class ModelBase:
     def write_vocab(self):
         raise NotImplementedError("write_vocab() must be implemented in subclasses")
 
+    def write_extra_tensors(self):
+        """Append model-specific tensors that are not present in the HF model state dict."""
+
     def write(self):
         self.prepare_tensors()
         self.prepare_metadata(vocab_only=False)
+        self.write_extra_tensors()
         self.gguf_writer.write_header_to_file(path=self.fname_out)
         self.gguf_writer.write_kv_data_to_file()
         self.gguf_writer.write_tensors_to_file(progress=True)
diff --git a/conversion/qwen3vl.py b/conversion/qwen3vl.py
index 4fec708..7d883be 100644
--- a/conversion/qwen3vl.py
+++ b/conversion/qwen3vl.py
@@ -2,6 +2,8 @@ from __future__ import annotations
 
 import json
 
+import torch
+
 from typing import Any, Callable, Iterable, TYPE_CHECKING
 
 if TYPE_CHECKING:
@@ -357,12 +359,57 @@ class Qwen3ASRTextModel(Qwen3VLTextModel):
         super().set_vocab()
         # fix chat template, use correct chatml format
         self.gguf_writer.add_chat_template("{% for message in messages %}{{'<|im_start|>' + message['role'] + '\\n' + message['content'] + '<|im_end|>' + '\\n'}}{% endfor %}{% if add_generation_prompt %}{{ '<|im_start|>assistant\\n' }}{% endif %}")
-        # correct BOS/EOS tokens
+        # Match tokenizer special IDs used by Qwen3-ASR's processor and Engram padding.
         with open(self.dir_model / "tokenizer_config.json", "r", encoding="utf-8") as f:
             tokenizer_config = json.load(f)
-            added_tokens = tokenizer_config.get("added_tokens_decoder", {})
-            for token_id, data in added_tokens.items():
-                if data.get("content") == "<|im_end|>":
-                    self.gguf_writer.add_bos_token_id(int(token_id))
-                    self.gguf_writer.add_eos_token_id(int(token_id))
-                    break
+        pad_token = tokenizer_config.get("pad_token")
+        if isinstance(pad_token, dict):
+            pad_token = pad_token.get("content")
+        for token_id, data in tokenizer_config.get("added_tokens_decoder", {}).items():
+            if data.get("content") == pad_token:
+                self.gguf_writer.add_pad_token_id(int(token_id))
+            if data.get("content") == "<|im_end|>":
+                self.gguf_writer.add_bos_token_id(int(token_id))
+                self.gguf_writer.add_eos_token_id(int(token_id))
+
+    def write_extra_tensors(self):
+        checkpoint_path = getattr(self, "engram_checkpoint", None)
+        if checkpoint_path is None:
+            return
+
+        checkpoint = torch.load(checkpoint_path, map_location="cpu", weights_only=False)
+        config = checkpoint["config"]
+        layer = int(config["layers"][0])
+        if (layer != 2 or config["buckets"] != {2: 10000, 3: 2000}
+                or int(config["memory_dim"]) != 512 or int(config["heads"]) != 16
+                or int(config["kernel"]) != 4):
+            raise ValueError("llama.cpp Engram export currently supports the project layer-2 512d/16-head checkpoint only")
+
+        states = checkpoint["engram_state"]
+        state = states.get(layer, states.get(str(layer)))
+        if state is None:
+            raise ValueError(f"checkpoint has no Engram state for layer {layer}")
+
+        def add(name, tensor, dtype):
+            data = tensor.detach().cpu().contiguous().numpy()
+            self.gguf_writer.add_tensor(name, data, raw_dtype=dtype)
+
+        table_id = 0
+        for order in (2, 3):
+            for head in range(16):
+                add(f"blk.{layer}.engram_table.{table_id}.weight",
+                    state[f"tables.n{order}_h{head}.weight"], gguf.GGMLQuantizationType.F32)
+                table_id += 1
+
+        for name in ("key_proj", "value_proj"):
+            add(f"blk.{layer}.engram_{name}.weight", state[f"{name}.weight"], gguf.GGMLQuantizationType.F32)
+            add(f"blk.{layer}.engram_{name}.bias", state[f"{name}.bias"], gguf.GGMLQuantizationType.F32)
+        for name in ("key_norm", "query_norm"):
+            add(f"blk.{layer}.engram_{name}.weight", state[f"{name}.weight"], gguf.GGMLQuantizationType.F32)
+
+        # GGML stores the kernel index as the second dimension and channels contiguously.
+        conv = state["short_conv.weight"][:, 0, :].transpose(0, 1).contiguous()
+        add(f"blk.{layer}.engram_short_conv.weight", conv, gguf.GGMLQuantizationType.F32)
+        seeds = state["hash_seeds"].to(torch.int64)
+        seed_parts = torch.stack((seeds & 0xffff, seeds >> 16), dim=-1).to(torch.float32)
+        add(f"blk.{layer}.engram_hash_seeds.weight", seed_parts, gguf.GGMLQuantizationType.F32)
diff --git a/convert_hf_to_gguf.py b/convert_hf_to_gguf.py
index e09616b..0ad390f 100755
--- a/convert_hf_to_gguf.py
+++ b/convert_hf_to_gguf.py
@@ -169,6 +169,10 @@ def parse_args() -> argparse.Namespace:
             "layer count to populate its GGUF."
         ),
     )
+    parser.add_argument(
+        "--engram-checkpoint", type=Path, default=None,
+        help="append the project's supported Qwen3-ASR layer-2 Engram checkpoint tensors",
+    )
 
     args = parser.parse_args()
     if not args.print_supported_models and args.model is None:
@@ -297,6 +301,11 @@ def main() -> None:
                                      fuse_qkv=args.fuse_qkv,
                                      )
 
+        if args.engram_checkpoint is not None:
+            if model_class.__name__ != "Qwen3ASRTextModel":
+                raise ValueError("--engram-checkpoint is supported only for Qwen3ASRForConditionalGeneration")
+            model_instance.engram_checkpoint = args.engram_checkpoint
+
         if args.vocab_only:
             logger.info("Exporting model vocab...")
             model_instance.write_vocab()
diff --git a/src/llama-arch.cpp b/src/llama-arch.cpp
index 03b7951..284dc3e 100644
--- a/src/llama-arch.cpp
+++ b/src/llama-arch.cpp
@@ -459,6 +459,13 @@ static const std::map<llm_tensor, const char *> LLM_TENSOR_NAMES = {
     { LLM_TENSOR_ATTN_Q_NORM,                            "blk.%d.attn_q_norm" },
     { LLM_TENSOR_ATTN_K_NORM,                            "blk.%d.attn_k_norm" },
     { LLM_TENSOR_ATTN_GATE,                              "blk.%d.attn_gate" },
+    { LLM_TENSOR_ENGRAM_TABLE,                           "blk.%d.engram_table.%d" },
+    { LLM_TENSOR_ENGRAM_KEY_PROJ,                        "blk.%d.engram_key_proj" },
+    { LLM_TENSOR_ENGRAM_VALUE_PROJ,                      "blk.%d.engram_value_proj" },
+    { LLM_TENSOR_ENGRAM_KEY_NORM,                        "blk.%d.engram_key_norm" },
+    { LLM_TENSOR_ENGRAM_QUERY_NORM,                      "blk.%d.engram_query_norm" },
+    { LLM_TENSOR_ENGRAM_SHORT_CONV,                      "blk.%d.engram_short_conv" },
+    { LLM_TENSOR_ENGRAM_HASH_SEEDS,                      "blk.%d.engram_hash_seeds" },
     { LLM_TENSOR_FFN_POST_NORM,                          "blk.%d.post_ffw_norm" },
     { LLM_TENSOR_FFN_POST_NORM_1,                        "blk.%d.post_ffw_norm_1" },
     { LLM_TENSOR_FFN_POST_NORM_2,                        "blk.%d.post_ffw_norm_2" },
@@ -741,6 +748,13 @@ static const std::map<llm_tensor, llm_tensor_info> LLM_TENSOR_INFOS = {
     {LLM_TENSOR_ATTN_QKV,                   {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
     {LLM_TENSOR_ATTN_OUT,                   {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
     {LLM_TENSOR_ATTN_GATE,                  {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
+    {LLM_TENSOR_ENGRAM_TABLE,               {LLM_TENSOR_LAYER_REPEATING, GGML_OP_GET_ROWS}},
+    {LLM_TENSOR_ENGRAM_KEY_PROJ,            {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
+    {LLM_TENSOR_ENGRAM_VALUE_PROJ,          {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
+    {LLM_TENSOR_ENGRAM_KEY_NORM,            {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL}},
+    {LLM_TENSOR_ENGRAM_QUERY_NORM,          {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL}},
+    {LLM_TENSOR_ENGRAM_SHORT_CONV,          {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL}},
+    {LLM_TENSOR_ENGRAM_HASH_SEEDS,          {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL}},
     {LLM_TENSOR_FFN_GATE,                   {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
     {LLM_TENSOR_FFN_DOWN,                   {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
     {LLM_TENSOR_FFN_UP,                     {LLM_TENSOR_LAYER_REPEATING, GGML_OP_MUL_MAT}},
diff --git a/src/llama-arch.h b/src/llama-arch.h
index af64f4e..40a2ef3 100644
--- a/src/llama-arch.h
+++ b/src/llama-arch.h
@@ -460,6 +460,13 @@ enum llm_tensor {
     LLM_TENSOR_ATTN_ROT_EMBD,
     LLM_TENSOR_ATTN_SINKS,
     LLM_TENSOR_ATTN_GATE,
+    LLM_TENSOR_ENGRAM_TABLE,
+    LLM_TENSOR_ENGRAM_KEY_PROJ,
+    LLM_TENSOR_ENGRAM_VALUE_PROJ,
+    LLM_TENSOR_ENGRAM_KEY_NORM,
+    LLM_TENSOR_ENGRAM_QUERY_NORM,
+    LLM_TENSOR_ENGRAM_SHORT_CONV,
+    LLM_TENSOR_ENGRAM_HASH_SEEDS,
     LLM_TENSOR_FFN_GATE_INP,
     LLM_TENSOR_FFN_GATE_INP_SHEXP,
     LLM_TENSOR_FFN_NORM,
diff --git a/src/llama-context.cpp b/src/llama-context.cpp
index fcd4dfb..6681969 100644
--- a/src/llama-context.cpp
+++ b/src/llama-context.cpp
@@ -16,6 +16,8 @@
 #include <cinttypes>
 #include <cmath>
 #include <cstring>
+#include <cstdlib>
+#include <fstream>
 #include <limits>
 #include <stdexcept>
 #include <string>
@@ -1767,6 +1769,30 @@ int llama_context::decode(const llama_batch & batch_inp) {
         return -1;
     }
 
+    if (model.arch == LLM_ARCH_QWEN3VL) {
+        const auto & normalized = balloc->get_batch();
+        std::map<llama_seq_id, llama_pos> max_pos;
+        std::map<llama_seq_id, bool> has_output;
+        std::set<llama_seq_id> reset_seq;
+        for (int32_t i = 0; i < normalized.n_tokens; ++i) {
+            const llama_pos pos = normalized.pos[i];
+            const int32_t n_seq = normalized.n_seq_id ? normalized.n_seq_id[i] : 1;
+            for (int32_t s = 0; s < n_seq; ++s) {
+                const llama_seq_id sid = normalized.seq_id ? normalized.seq_id[i][s] : 0;
+                auto it = max_pos.find(sid);
+                if (it == max_pos.end() || pos > it->second) max_pos[sid] = pos;
+                has_output[sid] = has_output[sid] || (normalized.logits && normalized.logits[i] != 0);
+                if (pos == 0) reset_seq.insert(sid);
+            }
+        }
+        for (const auto & [sid, max_position] : max_pos) {
+            auto & seq = engram_state.sequences[sid];
+            if (reset_seq.count(sid) != 0) seq = {};
+            if (!seq.prompt_complete && max_position > seq.prompt_end) seq.prompt_end = max_position;
+            if (has_output[sid]) seq.prompt_complete = true;
+        }
+    }
+
     const uint32_t n_tokens_all  = balloc->get_n_tokens();
     const uint32_t n_outputs_all = balloc->get_n_outputs();
 
@@ -1922,6 +1948,62 @@ int llama_context::decode(const llama_batch & batch_inp) {
         //    ggml_graph_dump_dot(gf, NULL, "llama.dot");
         //}
 
+        if (ggml_tensor * t_values = res->get_engram_values()) {
+            const int64_t engram_dim = t_values->ne[0];
+            const llama_token audio_pad_id = model.vocab.text_to_token("<|audio_pad|>");
+            GGML_ASSERT(ubatch.token != nullptr || (ubatch.embd != nullptr && audio_pad_id != LLAMA_TOKEN_NULL));
+            GGML_ASSERT(t_values->type == GGML_TYPE_F32);
+            GGML_ASSERT(t_values->ne[1] == ubatch.n_tokens);
+            std::vector<float> values(engram_dim * ubatch.n_tokens);
+            ggml_backend_tensor_get(t_values, values.data(), 0, values.size() * sizeof(float));
+
+            if (const char * dump_path = std::getenv("LLAMA_ENGRAM_DUMP")) {
+                ggml_tensor * t_hidden = res->get_engram_hidden();
+                GGML_ASSERT(t_hidden != nullptr && t_hidden->type == GGML_TYPE_F32 && t_hidden->ne[0] == engram_dim);
+                std::vector<float> hidden(engram_dim * ubatch.n_tokens);
+                ggml_backend_tensor_get(t_hidden, hidden.data(), 0, hidden.size() * sizeof(float));
+                std::ofstream dump(dump_path, std::ios::binary | std::ios::app);
+                const uint32_t n_tokens = ubatch.n_tokens;
+                const uint32_t n_embd = engram_dim;
+                dump.write(reinterpret_cast<const char *>(&n_tokens), sizeof(n_tokens));
+                dump.write(reinterpret_cast<const char *>(&n_embd), sizeof(n_embd));
+                for (uint32_t i = 0; i < ubatch.n_tokens; ++i) {
+                    const llama_pos pos = ubatch.pos[i];
+                    const int32_t token = ubatch.token ? ubatch.token[i] : audio_pad_id;
+                    const int32_t n_seq = ubatch.n_seq_id ? ubatch.n_seq_id[i] : 1;
+                    const llama_seq_id sid = n_seq > 0 && ubatch.seq_id ? ubatch.seq_id[i][0] : 0;
+                    const auto & seq = engram_state.sequences[sid];
+                    const uint8_t active = seq.prompt_complete && pos >= seq.prompt_end;
+                    dump.write(reinterpret_cast<const char *>(&pos), sizeof(pos));
+                    dump.write(reinterpret_cast<const char *>(&token), sizeof(token));
+                    dump.write(reinterpret_cast<const char *>(&active), sizeof(active));
+                }
+                dump.write(reinterpret_cast<const char *>(hidden.data()), hidden.size() * sizeof(float));
+                dump.write(reinterpret_cast<const char *>(values.data()), values.size() * sizeof(float));
+            }
+
+            std::map<llama_seq_id, llama_pos> latest_pos;
+            for (uint32_t i = 0; i < ubatch.n_tokens; ++i) {
+                const llama_pos pos = ubatch.pos[i];
+                const int32_t n_seq = ubatch.n_seq_id ? ubatch.n_seq_id[i] : 1;
+                for (int32_t s = 0; s < n_seq; ++s) {
+                    const llama_seq_id sid = ubatch.seq_id ? ubatch.seq_id[i][s] : 0;
+                    auto & seq = engram_state.sequences[sid];
+                    seq.tokens[pos] = ubatch.token ? ubatch.token[i] : audio_pad_id;
+                    const float * begin = values.data() + i * engram_dim;
+                    seq.values[pos] = std::vector<float>(begin, begin + engram_dim);
+                    auto it = latest_pos.find(sid);
+                    if (it == latest_pos.end() || pos > it->second) latest_pos[sid] = pos;
+                }
+            }
+
+            for (const auto & [sid, latest] : latest_pos) {
+                auto & seq = engram_state.sequences[sid];
+                while (!seq.tokens.empty() && seq.tokens.begin()->first < latest - 2) seq.tokens.erase(seq.tokens.begin());
+                while (!seq.values.empty() && seq.values.begin()->first < latest - 9) seq.values.erase(seq.values.begin());
+            }
+        }
+
         auto * t_logits  = res->get_logits();
         auto * t_embd    = cparams.embeddings       ? res->get_embd()     : nullptr;
         auto * t_h_nextn = cparams.embeddings_nextn ? res->get_h_nextn()  : nullptr;
@@ -2559,6 +2641,7 @@ llm_graph_params llama_context::graph_params(
         /*.loras       =*/ loras.get(),
         /*.mctx        =*/ mctx,
         /*.cross       =*/ &cross,
+        /*.engram_state=*/ &engram_state,
         /*.samplers    =*/ sampling.samplers,
         /*.n_outputs   =*/ n_outputs,
         /*.cb          =*/ graph_get_cb(),
diff --git a/src/llama-context.h b/src/llama-context.h
index 77ef92f..51203a4 100644
--- a/src/llama-context.h
+++ b/src/llama-context.h
@@ -289,6 +289,8 @@ private:
 
     llama_cross cross; // TODO: tmp for handling cross-attention - need something better probably
 
+    llama_engram_context_state engram_state;
+
     llama_memory_ptr memory;
 
     // decode output (2-dimensional array: [n_outputs][n_vocab])
diff --git a/src/llama-graph.cpp b/src/llama-graph.cpp
index 07ca49a..e5edef1 100644
--- a/src/llama-graph.cpp
+++ b/src/llama-graph.cpp
@@ -1326,6 +1326,8 @@ void llm_graph_result::reset() {
     t_embd        = nullptr;
     t_embd_pooled = nullptr;
     t_h_nextn     = nullptr;
+    t_engram_values = nullptr;
+    t_engram_hidden = nullptr;
 
     t_layer_inp.resize(LLAMA_MAX_LAYERS + 1);
     std::fill(t_layer_inp.begin(), t_layer_inp.end(), nullptr);
@@ -1372,6 +1374,12 @@ void llm_graph_result::set_outputs(const llm_graph_params & params) {
     if (t_h_nextn != nullptr) {
         ggml_set_output(t_h_nextn);
     }
+    if (t_engram_values != nullptr) {
+        ggml_set_output(t_engram_values);
+    }
+    if (t_engram_hidden != nullptr) {
+        ggml_set_output(t_engram_hidden);
+    }
     {
         const auto & embeddings_layer_inp = params.cparams.embeddings_layer_inp;
         for (size_t il = 0; il < embeddings_layer_inp.size(); ++il) {
diff --git a/src/llama-graph.h b/src/llama-graph.h
index cc41106..8fcc8c6 100644
--- a/src/llama-graph.h
+++ b/src/llama-graph.h
@@ -33,6 +33,17 @@ class llama_memory_recurrent_context;
 class llama_memory_hybrid_context;
 class llama_memory_hybrid_iswa_context;
 
+struct llama_engram_sequence_state {
+    llama_pos prompt_end = -1;
+    bool prompt_complete = false;
+    std::map<llama_pos, llama_token> tokens;
+    std::map<llama_pos, std::vector<float>> values;
+};
+
+struct llama_engram_context_state {
+    std::map<llama_seq_id, llama_engram_sequence_state> sequences;
+};
+
 // certain models (typically multi-modal) can produce different types of graphs
 enum llm_graph_type {
     LLM_GRAPH_TYPE_DEFAULT,
@@ -786,6 +797,7 @@ struct llm_graph_params {
     const llama_adapter_loras    * loras;
     const llama_memory_context_i * mctx;
     const llama_cross            * cross;
+    const llama_engram_context_state * engram_state;
 
     std::map<llama_seq_id, llama_sampler *> samplers;
 
@@ -880,7 +892,8 @@ struct llm_graph_params {
             gtype == other.gtype &&
             cvec  == other.cvec  &&
             loras == other.loras &&
-            cross == other.cross;
+            cross == other.cross &&
+            engram_state == other.engram_state;
     }
 };
 
@@ -901,8 +914,10 @@ public:
     ggml_tensor * get_embd()        const { return t_embd; }
     ggml_tensor * get_embd_pooled() const { return t_embd_pooled; }
     ggml_tensor * get_h_nextn()     const { return t_h_nextn; }
+    ggml_tensor * get_engram_values() const { return t_engram_values; }
+    ggml_tensor * get_engram_hidden() const { return t_engram_hidden; }
 
-    ggml_tensor * get_layer_inp(int il) const { return t_layer_inp[il]; }
+    ggml_tensor * get_layer_inp(int il) const { return t_layer_inp[il];}
 
     ggml_cgraph  * get_gf()  const { return gf; }
     ggml_context * get_ctx() const { return ctx_compute.get(); }
@@ -936,6 +951,8 @@ public:
     ggml_tensor * t_embd        = nullptr;
     ggml_tensor * t_embd_pooled = nullptr;
     ggml_tensor * t_h_nextn     = nullptr; // [n_embd, n_outputs] hidden state before final output norm
+    ggml_tensor * t_engram_values = nullptr; // [n_embd, n_tokens]
+    ggml_tensor * t_engram_hidden = nullptr; // debug-only [n_embd, n_tokens]
 
     std::vector<ggml_tensor *> t_layer_inp;
 
diff --git a/src/models/models.h b/src/models/models.h
index 3f9c67c..0b491cb 100644
--- a/src/models/models.h
+++ b/src/models/models.h
@@ -599,6 +599,27 @@ struct llama_model_qwen3moe : public llama_model_base {
 struct llama_model_qwen3vl : public llama_model_base {
     llama_model_qwen3vl(const struct llama_model_params & params) : llama_model_base(params) {}
     void load_arch_hparams(llama_model_loader & ml) override;
+
+    struct {
+        bool enabled = false;
+        int layer = 2;
+        int heads = 16;
+        int head_dim = 32;
+        int memory_dim = 512;
+        int buckets_n2 = 10000;
+        int buckets_n3 = 2000;
+        int kernel = 4;
+        int dilation = 3;
+        ggml_tensor * tables[32] = {};
+        ggml_tensor * key_proj = nullptr;
+        ggml_tensor * key_proj_b = nullptr;
+        ggml_tensor * value_proj = nullptr;
+        ggml_tensor * value_proj_b = nullptr;
+        ggml_tensor * key_norm = nullptr;
+        ggml_tensor * query_norm = nullptr;
+        ggml_tensor * short_conv = nullptr;
+        ggml_tensor * hash_seeds = nullptr;
+    } engram;
     void load_arch_tensors(llama_model_loader & ml) override;
 
     struct graph : public llm_graph_context {
diff --git a/src/models/qwen3vl.cpp b/src/models/qwen3vl.cpp
index 30c08ed..c7cbc1f 100644
--- a/src/models/qwen3vl.cpp
+++ b/src/models/qwen3vl.cpp
@@ -1,5 +1,157 @@
 #include "models.h"
 
+#include <array>
+#include <cstdint>
+#include <cstring>
+#include <limits>
+#include <map>
+#include <vector>
+
+namespace {
+
+class llm_graph_input_engram final : public llm_graph_input_i {
+public:
+    llm_graph_input_engram(
+            const llama_model_qwen3vl & model,
+            const llama_engram_context_state * state,
+            ggml_tensor * indices,
+            ggml_tensor * conv_indices,
+            ggml_tensor * conv_masks,
+            ggml_tensor * conv_history,
+            ggml_tensor * active,
+            int64_t n_embd) :
+        model(model), state(state), indices(indices), conv_indices(conv_indices),
+        conv_masks(conv_masks), conv_history(conv_history), active(active), n_embd(n_embd) {}
+
+    void set_input(const llama_ubatch * ubatch) override {
+        GGML_ASSERT(ubatch->token != nullptr || ubatch->embd != nullptr);
+        GGML_ASSERT(state != nullptr);
+
+        if (!seeds_loaded) {
+            GGML_ASSERT(model.engram.hash_seeds != nullptr);
+            std::array<float, 384> seed_parts = {};
+            ggml_backend_tensor_get(model.engram.hash_seeds, seed_parts.data(), 0, sizeof(seed_parts));
+            for (size_t i = 0; i < seeds.size(); ++i) {
+                const uint64_t low = static_cast<uint64_t>(seed_parts[2 * i]);
+                const uint64_t high = static_cast<uint64_t>(seed_parts[2 * i + 1]);
+                seeds[i] = static_cast<int64_t>(low | (high << 16));
+            }
+            seeds_loaded = true;
+        }
+
+        const int64_t n_tokens = ubatch->n_tokens;
+        const int64_t n_tables = 2 * model.engram.heads;
+        std::vector<llama_token> audio_token_ids;
+        const llama_token * token_ids = ubatch->token;
+        if (token_ids == nullptr) {
+            // Qwen3-ASR expands each encoded acoustic position to one <|audio_pad|> input ID.
+            const llama_token audio_pad_id = model.vocab.text_to_token("<|audio_pad|>");
+            GGML_ASSERT(audio_pad_id != LLAMA_TOKEN_NULL && "Qwen3-ASR Engram needs the <|audio_pad|> token for audio embeddings");
+            audio_token_ids.assign(n_tokens, audio_pad_id);
+            token_ids = audio_token_ids.data();
+        }
+        llama_token pad_id = model.vocab.token_pad();
+        if (pad_id == LLAMA_TOKEN_NULL) {
+            pad_id = model.vocab.token_eos();
+            if (pad_id == LLAMA_TOKEN_NULL) pad_id = 0;
+        }
+        const int64_t n_taps = model.engram.kernel;
+        std::vector<int32_t> table_ids(n_tokens * n_tables, 0);
+        std::vector<int32_t> tap_ids(n_tokens * n_taps, 0);
+        std::vector<float> tap_masks(n_tokens * n_taps, 0.0f);
+        std::vector<float> tap_history(n_tokens * n_taps * n_embd, 0.0f);
+        std::vector<float> active_data(n_tokens, 0.0f);
+
+        std::map<std::pair<llama_seq_id, llama_pos>, int64_t> current;
+        for (int64_t i = 0; i < n_tokens; ++i) {
+            const int32_t n_seq = ubatch->n_seq_id ? ubatch->n_seq_id[i] : 1;
+            for (int32_t s = 0; s < n_seq; ++s) {
+                const llama_seq_id sid = ubatch->seq_id ? ubatch->seq_id[i][s] : 0;
+                current[{sid, ubatch->pos[i]}] = i;
+            }
+        }
+
+        for (int64_t i = 0; i < n_tokens; ++i) {
+            const llama_pos pos = ubatch->pos[i];
+            const int32_t n_seq = ubatch->n_seq_id ? ubatch->n_seq_id[i] : 1;
+            GGML_ASSERT(n_seq > 0);
+            const llama_seq_id sid = ubatch->seq_id ? ubatch->seq_id[i][0] : 0;
+            const auto seq_it = state->sequences.find(sid);
+            GGML_ASSERT(seq_it != state->sequences.end());
+            const auto & seq = seq_it->second;
+
+            active_data[i] = seq.prompt_complete && pos >= seq.prompt_end ? 1.0f : 0.0f;
+
+            for (int table = 0; table < n_tables; ++table) {
+                const int order = table < model.engram.heads ? 2 : 3;
+                const int head = table % model.engram.heads;
+                uint64_t mixed = 0;
+                for (int k = 0; k < order; ++k) {
+                    const llama_pos prior_pos = pos - k;
+                    llama_token prior = pad_id;
+                    auto cur_it = current.find({sid, prior_pos});
+                    if (cur_it != current.end()) {
+                        prior = token_ids[cur_it->second];
+                    } else if (prior_pos >= 0) {
+                        auto hist_it = seq.tokens.find(prior_pos);
+                        if (hist_it != seq.tokens.end()) {
+                            prior = hist_it->second;
+                        }
+                    }
+                    const int64_t seed_idx = (order * model.engram.heads + head) * 3 + k;
+                    mixed ^= static_cast<uint64_t>(prior) * static_cast<uint64_t>(seeds[seed_idx]);
+                }
+                int64_t signed_mixed;
+                std::memcpy(&signed_mixed, &mixed, sizeof(signed_mixed));
+                const int64_t buckets = table < model.engram.heads ? model.engram.buckets_n2 : model.engram.buckets_n3;
+                int64_t bucket = signed_mixed % buckets;
+                if (bucket < 0) bucket += buckets;
+                table_ids[table * n_tokens + i] = static_cast<int32_t>(bucket);
+            }
+
+            for (int tap = 0; tap < n_taps; ++tap) {
+                const llama_pos prior_pos = pos - (n_taps - 1 - tap) * model.engram.dilation;
+                const int64_t offset = tap * n_tokens + i;
+                auto cur_it = current.find({sid, prior_pos});
+                if (cur_it != current.end()) {
+                    tap_ids[offset] = static_cast<int32_t>(cur_it->second);
+                    tap_masks[offset] = 1.0f;
+                } else if (prior_pos >= 0) {
+                    auto hist_it = seq.values.find(prior_pos);
+                    if (hist_it != seq.values.end()) {
+                        GGML_ASSERT((int64_t) hist_it->second.size() == n_embd);
+                        std::copy(hist_it->second.begin(), hist_it->second.end(),
+                                tap_history.begin() + (tap * n_tokens + i) * n_embd);
+                    }
+                }
+            }
+        }
+
+        ggml_backend_tensor_set(indices, table_ids.data(), 0, table_ids.size() * sizeof(int32_t));
+        ggml_backend_tensor_set(conv_indices, tap_ids.data(), 0, tap_ids.size() * sizeof(int32_t));
+        ggml_backend_tensor_set(conv_masks, tap_masks.data(), 0, tap_masks.size() * sizeof(float));
+        ggml_backend_tensor_set(conv_history, tap_history.data(), 0, tap_history.size() * sizeof(float));
+        ggml_backend_tensor_set(active, active_data.data(), 0, active_data.size() * sizeof(float));
+    }
+
+    bool can_reuse(const llm_graph_params & params) override {
+        return params.engram_state == state && indices->ne[0] == params.ubatch.n_tokens;
+    }
+
+    const llama_model_qwen3vl & model;
+    const llama_engram_context_state * state;
+    ggml_tensor * indices;
+    ggml_tensor * conv_indices;
+    ggml_tensor * conv_masks;
+    ggml_tensor * conv_history;
+    ggml_tensor * active;
+    int64_t n_embd;
+    bool seeds_loaded = false;
+    std::array<int64_t, 192> seeds = {};
+};
+
+} // namespace
+
 void llama_model_qwen3vl::load_arch_hparams(llama_model_loader & ml) {
     ml.get_key(LLM_KV_NUM_DEEPSTACK_LAYERS, hparams.n_deepstack_layers, false);
     ml.get_key_or_arr(LLM_KV_ROPE_DIMENSION_SECTIONS, hparams.rope_sections, 4, true);
@@ -13,7 +165,7 @@ void llama_model_qwen3vl::load_arch_hparams(llama_model_loader & ml) {
     }
 }
 
-void llama_model_qwen3vl::load_arch_tensors(llama_model_loader &) {
+void llama_model_qwen3vl::load_arch_tensors(llama_model_loader & ml) {
     LLAMA_LOAD_LOCALS;
 
     int64_t n_vocab_out = n_vocab;
@@ -51,6 +203,31 @@ void llama_model_qwen3vl::load_arch_tensors(llama_model_loader &) {
         layer.ffn_down = create_tensor(tn(LLM_TENSOR_FFN_DOWN, "weight", i), {  n_ff, n_embd}, 0);
         layer.ffn_up   = create_tensor(tn(LLM_TENSOR_FFN_UP,   "weight", i), {n_embd,   n_ff}, 0);
     }
+
+    auto engram_key_name = tn(LLM_TENSOR_ENGRAM_KEY_PROJ, "weight", 2).str();
+    if (ml.get_tensor_meta(engram_key_name.c_str()) != nullptr) {
+        engram.enabled = true;
+        engram.key_proj = create_tensor(tn(LLM_TENSOR_ENGRAM_KEY_PROJ, "weight", engram.layer), {n_embd, n_embd}, 0);
+        engram.key_proj_b = create_tensor(tn(LLM_TENSOR_ENGRAM_KEY_PROJ, "bias", engram.layer), {n_embd}, 0);
+        engram.value_proj = create_tensor(tn(LLM_TENSOR_ENGRAM_VALUE_PROJ, "weight", engram.layer), {2 * engram.memory_dim, n_embd}, 0);
+        engram.value_proj_b = create_tensor(tn(LLM_TENSOR_ENGRAM_VALUE_PROJ, "bias", engram.layer), {n_embd}, 0);
+        engram.key_norm = create_tensor(tn(LLM_TENSOR_ENGRAM_KEY_NORM, "weight", engram.layer), {n_embd}, 0);
+        engram.query_norm = create_tensor(tn(LLM_TENSOR_ENGRAM_QUERY_NORM, "weight", engram.layer), {n_embd}, 0);
+        engram.short_conv = create_tensor(tn(LLM_TENSOR_ENGRAM_SHORT_CONV, "weight", engram.layer), {n_embd, engram.kernel}, 0);
+        engram.hash_seeds = create_tensor(tn(LLM_TENSOR_ENGRAM_HASH_SEEDS, "weight", engram.layer), {2, 3, engram.heads, 4}, 0);
+
+        int table_id = 0;
+        for (int order : {2, 3}) {
+            const int64_t buckets = order == 2 ? engram.buckets_n2 : engram.buckets_n3;
+            for (int head = 0; head < engram.heads; ++head) {
+                engram.tables[table_id] = create_tensor(
+                        tn(LLM_TENSOR_ENGRAM_TABLE, "weight", engram.layer, table_id),
+                        {engram.head_dim, buckets}, 0);
+                table_id++;
+            }
+        }
+        LLAMA_LOG_INFO("%s: loaded project Engram module at layer %d\n", __func__, engram.layer);
+    }
 }
 
 std::unique_ptr<llm_graph_context> llama_model_qwen3vl::build_arch_graph(const llm_graph_params & params) const {
@@ -71,6 +248,26 @@ llama_model_qwen3vl::graph::graph(const llama_model & model, const llm_graph_par
 
     inpL = build_inp_embd(model.tok_embd);
 
+    const auto & qwen_model = static_cast<const llama_model_qwen3vl &>(model);
+    llm_graph_input_engram * inp_engram = nullptr;
+    if (qwen_model.engram.enabled) {
+        GGML_ASSERT(params.engram_state != nullptr);
+        auto input = std::make_unique<llm_graph_input_engram>(
+                qwen_model, params.engram_state,
+                ggml_new_tensor_2d(ctx0, GGML_TYPE_I32, n_tokens, 2 * qwen_model.engram.heads),
+                ggml_new_tensor_2d(ctx0, GGML_TYPE_I32, n_tokens, qwen_model.engram.kernel),
+                ggml_new_tensor_2d(ctx0, GGML_TYPE_F32, n_tokens, qwen_model.engram.kernel),
+                ggml_new_tensor_3d(ctx0, GGML_TYPE_F32, n_embd, n_tokens, qwen_model.engram.kernel),
+                ggml_new_tensor_2d(ctx0, GGML_TYPE_F32, 1, n_tokens),
+                n_embd);
+        ggml_set_input(input->indices);
+        ggml_set_input(input->conv_indices);
+        ggml_set_input(input->conv_masks);
+        ggml_set_input(input->conv_history);
+        ggml_set_input(input->active);
+        inp_engram = static_cast<llm_graph_input_engram *>(res->add_input(std::move(input)));
+    }
+
     int sections[4];
     std::copy(std::begin(hparams.rope_sections), std::begin(hparams.rope_sections) + 4, sections);
 
@@ -147,6 +344,64 @@ llama_model_qwen3vl::graph::graph(const llama_model & model, const llm_graph_par
 
         cur = ggml_add(ctx0, cur, ffn_inp);
 
+        if (qwen_model.engram.enabled && il == qwen_model.engram.layer) {
+            GGML_ASSERT(inp_engram != nullptr);
+
+            ggml_tensor * memory = nullptr;
+            for (int table = 0; table < 2 * qwen_model.engram.heads; ++table) {
+                ggml_tensor * ids = ggml_view_1d(ctx0, inp_engram->indices, n_tokens,
+                        table * n_tokens * sizeof(int32_t));
+                ggml_tensor * head_memory = ggml_get_rows(ctx0, qwen_model.engram.tables[table], ids);
+                memory = memory == nullptr ? head_memory : ggml_concat(ctx0, memory, head_memory, 0);
+            }
+            memory = ggml_cast(ctx0, memory, GGML_TYPE_F32);
+
+            res->t_engram_hidden = ggml_cast(ctx0, cur, GGML_TYPE_F32);
+            ggml_tensor * query = ggml_rms_norm(ctx0, res->t_engram_hidden, std::numeric_limits<float>::epsilon());
+            query = ggml_mul(ctx0, query, qwen_model.engram.query_norm);
+
+            ggml_tensor * key = ggml_mul_mat(ctx0, qwen_model.engram.key_proj, memory);
+            key = ggml_add(ctx0, key, qwen_model.engram.key_proj_b);
+            key = ggml_rms_norm(ctx0, key, std::numeric_limits<float>::epsilon());
+            key = ggml_mul(ctx0, key, qwen_model.engram.key_norm);
+
+            ggml_tensor * gate = ggml_sum_rows(ctx0, ggml_mul(ctx0, key, query));
+            gate = ggml_scale(ctx0, gate, 1.0f / sqrtf((float) n_embd));
+            gate = ggml_mul(ctx0,
+                    ggml_sgn(ctx0, gate),
+                    ggml_sqrt(ctx0, ggml_clamp(ctx0, ggml_abs(ctx0, gate), 1e-6f, 1e30f)));
+            gate = ggml_sigmoid(ctx0, gate);
+
+            ggml_tensor * value = ggml_mul_mat(ctx0, qwen_model.engram.value_proj, memory);
+            value = ggml_add(ctx0, value, qwen_model.engram.value_proj_b);
+            value = ggml_mul(ctx0, value, gate);
+            res->t_engram_values = value;
+
+            ggml_tensor * conv = nullptr;
+            for (int tap = 0; tap < qwen_model.engram.kernel; ++tap) {
+                const size_t tap_offset = tap * n_tokens * sizeof(float);
+                ggml_tensor * ids = ggml_view_1d(ctx0, inp_engram->conv_indices, n_tokens,
+                        tap * n_tokens * sizeof(int32_t));
+                ggml_tensor * mask = ggml_view_2d(ctx0, inp_engram->conv_masks, 1, n_tokens,
+                        sizeof(float), tap_offset);
+                ggml_tensor * current = ggml_mul(ctx0, ggml_get_rows(ctx0, value, ids), mask);
+                ggml_tensor * history = ggml_view_2d(ctx0, inp_engram->conv_history, n_embd, n_tokens,
+                        inp_engram->conv_history->nb[1], tap * n_embd * n_tokens * sizeof(float));
+                ggml_tensor * past = ggml_add(ctx0, current, history);
+                ggml_tensor * weight = ggml_view_1d(ctx0, qwen_model.engram.short_conv, n_embd,
+                        tap * n_embd * sizeof(float));
+                ggml_tensor * term = ggml_mul(ctx0, past, weight);
+                conv = conv == nullptr ? term : ggml_add(ctx0, conv, term);
+            }
+
+            ggml_tensor * active = inp_engram->active;
+            ggml_tensor * delta = ggml_add(ctx0, value, conv);
+            delta = ggml_mul(ctx0, delta, active);
+            delta = ggml_cast(ctx0, delta, cur->type);
+            cur = ggml_add(ctx0, cur, delta);
+            cb(cur, "engram_out", il);
+        }
+
         cur = build_cvec(cur, il);
         cb(cur, "l_out", il);
 
